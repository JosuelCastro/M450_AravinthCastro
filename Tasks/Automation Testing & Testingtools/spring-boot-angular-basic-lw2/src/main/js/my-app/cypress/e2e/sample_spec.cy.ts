describe('Sample E2E Test', () => {
  it('should navigate to the login page and verify the title', () => {
    cy.visit('http://localhost:4200'); // Adjust the URL based on your Angular app's URL
  });
});
